import { BasicWrapper } from "@/components";
import React from "react";
import { Text, View } from "react-native";

function Arena() {
  return (
    <View className="flex-1">
      <Text>Arena</Text>
    </View>
  );
}

export default BasicWrapper(Arena);
